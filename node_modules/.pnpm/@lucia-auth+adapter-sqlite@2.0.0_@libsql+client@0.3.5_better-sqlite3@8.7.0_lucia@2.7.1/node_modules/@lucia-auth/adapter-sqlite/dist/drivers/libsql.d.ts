import type { Adapter, InitializeAdapter } from "lucia";
import type { Client } from "@libsql/client";
export declare const libsqlAdapter: (db: Client, tables: {
    user: string;
    session: string | null;
    key: string;
}) => InitializeAdapter<Adapter>;
