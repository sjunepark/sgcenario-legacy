import type { Adapter, InitializeAdapter } from "lucia";
import type { Database } from "better-sqlite3";
export declare const betterSqlite3Adapter: (db: Database, tables: {
    user: string;
    session: string | null;
    key: string;
}) => InitializeAdapter<Adapter>;
