import type { Adapter, InitializeAdapter } from "lucia";
import type { D1Database } from "@cloudflare/workers-types";
export declare const d1Adapter: (db: D1Database, tables: {
    user: string;
    session: string | null;
    key: string;
}) => InitializeAdapter<Adapter>;
